
name: Learning

on:
  push:
    branches:
      - master

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        food:
          - pizza
          - beer
          - ice cream
          - salad
    steps:
      - name: Select thing
        run: |
          echo "$${ matrix.food }"
